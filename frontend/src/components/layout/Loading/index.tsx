import { StyledContainer, StyledSpinner } from "./Loading.styles";

const Loadding = () => {
  return (
    <StyledContainer className="loading">
      <StyledSpinner />
    </StyledContainer>
  );
};

export default Loadding;
